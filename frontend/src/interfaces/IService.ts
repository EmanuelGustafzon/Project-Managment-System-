export interface IService {
    id: number;
    name: string;
    currency: string;
    unit: string;
    price: number;
}
export interface IServiceForm {
    name: string;
    currency: string;
    unit: string;
    price: number;
}