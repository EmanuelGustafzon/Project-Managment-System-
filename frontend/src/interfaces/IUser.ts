export interface IUser {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
}
export interface IUserForm {
    firstName: string;
    lastName: string;
    email: string;
}